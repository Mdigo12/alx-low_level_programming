#include "main.h"

/**
 * main - Entry point
 *
 * Description: prints the alphabet, in lowercase, followed by a new line
 *
 * Return: 0 (Success)
 */

void print_alphabet(void)
{
	char lc;

	for (i = 'a'; i <= 'z'; i++)
	{
		_putchar(lc);
	}
	_putchar('\n');
}
