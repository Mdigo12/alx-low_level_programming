#include "main.h"

/**
 * int _abs - see description
 *
 * Description: computes the absolute value of an integer.
 *
 * Return: 0 (success)
 */

int _abs(int)
{
	int r;
	
	if (r < 0)
	{
		r = 0 - r;
	}
	else
		r = 0 + r;

	return (0);
}
